services:
  nats:
    image: nats:latest
    container_name: prompt-ver-nats
    ports:
      - "4222:4222"   # client connections
      - "8222:8222"   # HTTP monitoring
    command: ["--jetstream", "--http_port", "8222"]
    restart: unless-stopped
